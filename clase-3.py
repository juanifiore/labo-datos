import numpy as np

'''
def pisar_elemento(M,e):
    for i in range len(M):
        for j in range len(M[0]):
            if M[i][j] == e:
                M[i][j] == -1
    return M
'''

a = np.array([1,2,3],[4,5,6])
print(a.size())
